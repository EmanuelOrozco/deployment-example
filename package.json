{
  "name": "deployment-example",
  "version": "1.0.0",
  "description": "example for deployment",
  "author": "EmanuelOrozco",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "build": "tsc && cp src/*.html dist/ && cp src/*.css dist/ && cp src/*.png dist/"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}